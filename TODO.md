# Workshop TODO

- [ ] Dry-run the hooks workshop end-to-end: build the three hooks in parallel worktrees, test
      them, copy one into `~/.claude`, and confirm it fires in a fresh Claude Code session.
- [ ] Confirm `codacy-cli` setup timing on a clean machine; tune the setup guide if the first
      `codacy-cli install` is slow.
- [ ] Rehearse the new Hooks act and the demo transition slides.
